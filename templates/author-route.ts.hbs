/**
 * Next.js App Router API route for a beehiiv author
 * Generated by beehiiv-react — do not edit directly.
 * Located at: app/api/beehiiv/authors/[id]/route.ts
 */
import { NextResponse } from 'next/server';
import { createBeehiivClient } from 'beehiiv-react/server';

const client = createBeehiivClient({
  publicationId: process.env.BEEHIIV_PUBLICATION_ID ?? '{{publicationId}}',
});

/** GET /api/beehiiv/authors/[id] — Get one author */
export async function GET(
  _req: Request,
  { params }: { params: Promise<{ id: string }> },
) {
  try {
    const { id } = await params;
    const response = await client.authors.get(id);
    return NextResponse.json({ data: response.data }, { status: 200 });
  } catch (error: unknown) {
    const message =
      error instanceof Error ? error.message : 'Failed to fetch author';
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
