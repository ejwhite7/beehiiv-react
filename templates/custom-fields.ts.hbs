/**
 * Typed custom fields for {{publicationName}}
 * Auto-generated by `beehiiv-react init` / `beehiiv-react sync`
 * DO NOT EDIT MANUALLY — run `npx beehiiv-react sync` to refresh
 */

/** All custom field names defined in your beehiiv publication */
export type BeehiivCustomFieldName = {{#each fields}}'{{display}}'{{#unless @last}} | {{/unless}}{{/each}};

/** Strongly-typed custom field values for {{publicationName}} */
export interface BeehiivCustomFields {
  {{#each fields}}
  /** {{display}} ({{kind}}) */
  {{camelCase display}}?: {{tsType}};
  {{/each}}
}

/** Raw custom field definitions from beehiiv */
export const beehiivCustomFieldDefinitions = [
  {{#each fields}}
  {
    id: '{{id}}',
    display: '{{display}}',
    kind: '{{kind}}' as const,
    {{#if options}}
    options: [{{#each options}}'{{this}}'{{#unless @last}}, {{/unless}}{{/each}}],
    {{/if}}
  },
  {{/each}}
] as const;
